akka {
  loglevel = DEBUG
}

http {
  interface = "0.0.0.0"
  port = 9000
}

services {
  telizeHost = "www.telize.com"
  telizePort = 80
  zippopotamusHost = "www.zippopotam.us"
  zippopotamusPort = 80
}

